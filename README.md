# Lab Experiment Controller UI

A Python GUI simulation to control and visualize mock lab experiments. Simulates real-time sensor input, allows parameter control, and plots data live.

## Features
- GUI with Tkinter
- Simulated sensor values (e.g., voltage, temperature)
- Data preprocessing (moving average)
- Export to CSV

## Run It

```bash
pip install -r requirements.txt
python main.py
